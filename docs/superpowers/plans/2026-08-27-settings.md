# Settings dùng chung — Kế hoạch triển khai

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Xây module Settings (`packages/views/settings/`) port shell Multica, gom profile/preferences/workspace/members/integrations; bỏ Members khỏi sidebar; thêm `PATCH /me` và `PATCH /workspaces/{id}`.

**Architecture:** Backend thêm sqlc + service methods + handlers; frontend thêm endpoints/hooks/permissions rồi port `settings-layout`, `use-auto-save`, `settings-page` (5 tab); `MembersView` embed qua prop `embedded`; route `/{org}/{ws}/settings?tab=…`, redirect `/members`.

**Tech Stack:** Go 1.27 (chi, pgx, sqlc) · Postgres 16 · Next.js App Router · React 19 · TanStack Query 5 · zod · i18next · vitest · Playwright (e2e optional).

**Spec:** `docs/superpowers/specs/2026-08-27-settings-design.md`

## Global Constraints

- Chạy lệnh từ repo root (`make help`). Sau sửa `server/pkg/db/queries/*.sql`: `make sqlc` + commit generated.
- Go module `github.com/unicomhub/uniwork/server`. ID ULID text (`util.NewID()`).
- API error envelope `{"error":{"code","message"}}`; map qua `mapServiceError`.
- FE packages: `@uniwork/core`, `@uniwork/ui`, `@uniwork/views`. Views **không** import `next/*`; navigate qua `useNavigation()` / `<AppLink>`.
- UI: semantic tokens only (`bg-background`, `text-muted-foreground`, `border-border`); **không** `--uw-*` / legacy utilities (`scripts/no-legacy-tokens.test.mjs`).
- Mọi text JSX trong `packages/views/` qua `t()` (`i18next/no-literal-string`). Parity **vi + en** (`packages/core/i18n/parity.test.ts`).
- Endpoint JSON: `parseWithFallback` + zod schema + malformed-response test (`CLAUDE.md` API Compatibility).
- Views tests mock `@uniwork/core/api/http`, không mock `next/*`.
- Commit conventional prefix (`feat(settings)`, `fix(settings)`, …). TDD: test trước khi có logic hành vi.
- Tham chiếu layout Multica tại `multica/packages/views/settings/` (local, gitignored) — **không** commit string `multica` vào source (`scripts/no-usf-leak.test.mjs`).

## Bản đồ file

**Backend**
- `server/pkg/db/queries/users.sql` — `UpdateUserDisplayName`
- `server/pkg/db/queries/workspaces.sql` — `UpdateWorkspaceName`
- `server/internal/service/auth.go` — `UpdateProfile`
- `server/internal/service/workspace.go` — `Update`, `UpdateWorkspaceInput`
- `server/internal/handler/auth.go` — `patchMe`
- `server/internal/handler/workspace.go` — `patchWorkspace`
- `server/internal/handler/router.go` — routes
- `server/internal/service/*_test.go`, `server/internal/handler/*_test.go`

**packages/core**
- `api/endpoints/auth.ts`, `auth.test.ts` — `patchMe`, `uploadAvatar`
- `api/endpoints/workspaces.ts`, `workspaces.test.ts` — `patchWorkspace`
- `workspaces/hooks.ts` — `usePatchWorkspace`
- `auth/hooks.ts` hoặc `settings`-adjacent — `usePatchMe` (nếu chưa có auth hooks file, thêm mutation vào `packages/core/auth/` hoặc inline trong views qua endpoint + auth store)
- `permissions/rules.ts`, `rules.test.ts` — `canUpdateWorkspaceSettings`
- `paths/paths.ts`, `paths/consistency.test.ts`
- `i18n/locales/{vi,en}.json` — namespace `settings`, `nav.settings`

**packages/views**
- `settings/components/*` (mới)
- `workspace/members-view.tsx` — prop `embedded?: boolean`
- `layout/app-sidebar.tsx`, `app-sidebar.test.tsx`

**apps/web**
- `app/[orgSlug]/[workspaceSlug]/settings/page.tsx` (mới)
- `app/[orgSlug]/[workspaceSlug]/members/page.tsx` — redirect

**e2e (optional)**
- `e2e/settings.spec.ts`

---

## Phase A — Backend

### Task 1: PATCH /api/v1/me

**Files:**
- Modify: `server/pkg/db/queries/users.sql`
- Modify: `server/internal/service/auth.go`
- Modify: `server/internal/service/auth_test.go` (hoặc tạo nếu chưa có)
- Modify: `server/internal/handler/auth.go`
- Modify: `server/internal/handler/router.go`
- Modify: `server/internal/handler/auth_test.go`
- Regenerate: `server/pkg/db/generated/users.sql.go`

**Interfaces:**
- Produces: `AuthService.UpdateProfile(ctx, userID, displayName string) (db.User, error)`
- Produces: `Queries.UpdateUserDisplayName(ctx, db.UpdateUserDisplayNameParams) (db.User, error)`
- Handler: `PATCH /api/v1/me` body `{ "display_name": "..." }` → `{ "user": userDTO }`

- [ ] **Step 1: Thêm sqlc query**

Trong `server/pkg/db/queries/users.sql`:

```sql
-- name: UpdateUserDisplayName :one
UPDATE users SET display_name = $2, updated_at = now()
WHERE id = $1
RETURNING *;
```

Chạy: `make sqlc`

- [ ] **Step 2: Viết test service (fail trước)**

Trong `server/internal/service/auth_test.go`:

```go
func TestUpdateProfile(t *testing.T) {
	ctx := context.Background()
	s, u := testAuthService(t)

	updated, err := s.UpdateProfile(ctx, u.ID, "  New Name  ")
	if err != nil {
		t.Fatal(err)
	}
	if updated.DisplayName != "New Name" {
		t.Fatalf("display_name = %q", updated.DisplayName)
	}

	_, err = s.UpdateProfile(ctx, u.ID, "   ")
	if !errors.Is(err, Invalid("")) && err.Error() == "" {
		// hoặc dùng errors.As Invalid — mirror pattern Register validation
	}
}
```

- [ ] **Step 3: Implement `UpdateProfile`**

```go
const maxDisplayNameRunes = 100

func (s *AuthService) UpdateProfile(ctx context.Context, userID, displayName string) (db.User, error) {
	name := strings.TrimSpace(displayName)
	if name == "" {
		return db.User{}, Invalid("tên hiển thị không được để trống")
	}
	if utf8.RuneCountInString(name) > maxDisplayNameRunes {
		return db.User{}, Invalid("tên hiển thị quá dài")
	}
	u, err := s.q.UpdateUserDisplayName(ctx, db.UpdateUserDisplayNameParams{ID: userID, DisplayName: name})
	if errors.Is(err, pgx.ErrNoRows) {
		return db.User{}, ErrNotFound
	}
	return u, err
}
```

- [ ] **Step 4: Handler + route**

`auth.go`:

```go
func (h *handlers) patchMe(w http.ResponseWriter, r *http.Request) {
	var in struct {
		DisplayName *string `json:"display_name"`
	}
	if !decode(w, r, &in, maxJSONBody) {
		return
	}
	if in.DisplayName == nil {
		respondError(w, 400, "invalid_request", "display_name is required")
		return
	}
	u, err := h.Auth.UpdateProfile(r.Context(), middleware.UserID(r.Context()), *in.DisplayName)
	if err != nil {
		h.mapServiceError(w, err)
		return
	}
	respondJSON(w, 200, map[string]any{"user": toUserDTO(u)})
}
```

`router.go` trong auth group: `r.Patch("/me", h.patchMe)`

- [ ] **Step 5: Handler test**

Trong `auth_test.go`, sau login lấy token:

```go
res, out := doJSON(t, srv, "PATCH", "/api/v1/me", token, map[string]string{"display_name": "Renamed"})
if res.StatusCode != 200 {
	t.Fatalf("status %d", res.StatusCode)
}
if out.User.DisplayName != "Renamed" {
	t.Fatalf("got %q", out.User.DisplayName)
}
```

- [ ] **Step 6: Run Go tests**

Run: `cd server && go test ./internal/service/... ./internal/handler/... -run 'UpdateProfile|patchMe|Auth' -count=1`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add server/pkg/db/queries/users.sql server/pkg/db/generated/ server/internal/service/auth.go server/internal/service/auth_test.go server/internal/handler/auth.go server/internal/handler/router.go server/internal/handler/auth_test.go
git commit -m "feat(server): add PATCH /api/v1/me for display_name"
```

---

### Task 2: PATCH /api/v1/workspaces/{workspaceID}

**Files:**
- Modify: `server/pkg/db/queries/workspaces.sql`
- Modify: `server/internal/service/workspace.go`
- Modify: `server/internal/service/workspace_test.go`
- Modify: `server/internal/handler/workspace.go`
- Modify: `server/internal/handler/router.go`
- Modify: `server/internal/handler/workspace_test.go` (hoặc file test handler workspace hiện có)
- Regenerate: `server/pkg/db/generated/workspaces.sql.go`

**Interfaces:**
- Consumes: `WorkspaceService.RequireMember`, gate admin (mirror `InviteMany` line ~146)
- Produces: `WorkspaceService.Update(ctx, userID, workspaceID, UpdateWorkspaceInput) (WorkspaceView, error)`
- Produces: `Queries.UpdateWorkspaceName(ctx, {ID, Name}) (db.Workspace, error)`
- Handler: `PATCH /workspaces/{workspaceID}` body `{ "name": "..." }` → `{ "workspace": workspaceDTO }`

- [ ] **Step 1: sqlc query**

```sql
-- name: UpdateWorkspaceName :one
UPDATE workspaces SET name = $2, updated_at = now()
WHERE id = $1
RETURNING *;
```

Run: `make sqlc`

- [ ] **Step 2: Service test (fail trước)**

```go
func TestWorkspaceUpdateName(t *testing.T) {
	// setup: owner + workspace via testutil
	// member role → ErrForbidden
	// admin/owner → name updated
}
```

- [ ] **Step 3: Implement `Update`**

```go
type UpdateWorkspaceInput struct {
	Name *string
}

func (s *WorkspaceService) Update(ctx context.Context, userID, workspaceID string, in UpdateWorkspaceInput) (WorkspaceView, error) {
	m, err := s.RequireMember(ctx, workspaceID, userID)
	if err != nil {
		return WorkspaceView{}, err
	}
	if m.Role != "owner" && m.Role != "admin" {
		// org implicit admin handled inside RequireMember / access — mirror InviteMany:
		// re-use same admin check as InviteMany
		return WorkspaceView{}, ErrForbidden
	}
	if in.Name == nil {
		return WorkspaceView{}, Invalid("name is required")
	}
	name := strings.TrimSpace(*in.Name)
	if name == "" {
		return WorkspaceView{}, Invalid("tên workspace không được để trống")
	}
	// ... UpdateWorkspaceName + GetWorkspaceWithOrg → WorkspaceView
}
```

**Lưu ý:** Đọc `InviteMany` để mirror org owner/admin implicit admin — copy cùng helper nếu có.

- [ ] **Step 4: Handler + route**

`router.go`: `r.Patch("/workspaces/{workspaceID}", h.patchWorkspace)`

- [ ] **Step 5: Handler integration test**

PATCH với member token → 403; admin → 200 + name changed.

- [ ] **Step 6: Run Go tests**

Run: `make test-go` hoặc `cd server && go test ./internal/service/... ./internal/handler/... -count=1`

- [ ] **Step 7: Commit**

```bash
git commit -m "feat(server): add PATCH /api/v1/workspaces/{id} for name"
```

---

## Phase B — Core frontend

### Task 3: Endpoints, hooks, permissions, paths

**Files:**
- Modify: `packages/core/api/endpoints/auth.ts`
- Modify: `packages/core/api/endpoints/auth.test.ts`
- Modify: `packages/core/api/endpoints/workspaces.ts`
- Modify: `packages/core/api/endpoints/workspaces.test.ts`
- Modify: `packages/core/workspaces/hooks.ts`
- Create: `packages/core/auth/mutations.ts` (hoặc extend file auth hiện có)
- Modify: `packages/core/permissions/rules.ts`
- Modify: `packages/core/permissions/rules.test.ts`
- Modify: `packages/core/paths/paths.ts`
- Modify: `packages/core/paths/consistency.test.ts`

**Interfaces:**
- Produces: `patchMe(body: { display_name: string }): Promise<User | null>`
- Produces: `uploadAvatar(file: File): Promise<User | null>` — `FormData` field `file`, `POST /api/v1/me/avatar`
- Produces: `patchWorkspace(id: string, body: { name: string }): Promise<Workspace | null>`
- Produces: `usePatchMe()`, `usePatchWorkspace(wsId: string)`
- Produces: `canUpdateWorkspaceSettings(ctx: PermissionContext): Decision`
- Produces: `paths.workspace(...).settings() → string`

- [ ] **Step 1: Failing endpoint tests**

`auth.test.ts`:

```typescript
it("patchMe returns user or null on malformed", async () => {
  vi.mocked(fetch)
    .mockResolvedValueOnce(json({ user: { ...user, display_name: "B" } }))
    .mockResolvedValueOnce(json({ nope: true }));
  expect((await patchMe({ display_name: "B" }))?.display_name).toBe("B");
  await expect(patchMe({ display_name: "B" })).resolves.toBeNull();
});
```

Tương tự `workspaces.test.ts` cho `patchWorkspace`.

- [ ] **Step 2: Implement endpoints**

```typescript
export async function patchMe(body: { display_name: string }): Promise<User | null> {
  const raw = await request("/api/v1/me", { method: "PATCH", body });
  return parseWithFallback<{ user: User } | null>(raw, UserResponse, null, {
    endpoint: "PATCH /api/v1/me",
  })?.user ?? null;
}

export async function uploadAvatar(file: File): Promise<User | null> {
  const form = new FormData();
  form.append("file", file);
  const raw = await request("/api/v1/me/avatar", { method: "POST", body: form });
  return parseWithFallback<{ user: User } | null>(raw, UserResponse, null, {
    endpoint: "POST /api/v1/me/avatar",
  })?.user ?? null;
}
```

- [ ] **Step 3: Mutations hooks**

```typescript
export function usePatchMe() {
  const setUser = useAuthStore((s) => s.setUser);
  return useMutation({
    mutationFn: (display_name: string) => patchMe({ display_name }),
    onSuccess: (u) => { if (u) setUser(u); },
  });
}

export function usePatchWorkspace(orgSlug: string, wsSlug: string) {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: ({ wsId, name }: { wsId: string; name: string }) => patchWorkspace(wsId, { name }),
    onSuccess: () => {
      void qc.invalidateQueries({ queryKey: workspaceKeys.list() });
      void qc.invalidateQueries({ queryKey: workspaceKeys.bySlugs(orgSlug, wsSlug) });
    },
  });
}
```

- [ ] **Step 4: Permission rule + test**

```typescript
export function canUpdateWorkspaceSettings(ctx: PermissionContext): Decision {
  const gate = requireWorkspaceMember(ctx);
  if (gate) return gate;
  if (isAdminLike(ctx.wsRole)) return ALLOW;
  return deny("not_admin_role", "Only workspace owners and admins can update workspace settings.");
}
```

- [ ] **Step 5: paths.settings()**

```typescript
settings: () => `${base}/settings`,
```

Cập nhật `builderTemplates()` trong `consistency.test.ts`.

- [ ] **Step 6: Run tests**

Run: `pnpm --filter @uniwork/core test`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git commit -m "feat(core): add settings API endpoints, hooks, and permissions"
```

---

## Phase C — Settings UI shell

### Task 4: settings-layout + use-auto-save

**Files:**
- Create: `packages/views/settings/components/settings-layout.tsx`
- Create: `packages/views/settings/components/settings-layout.test.tsx`
- Create: `packages/views/settings/components/use-auto-save.ts`
- Create: `packages/views/settings/components/use-auto-save.test.tsx`
- Create: `packages/views/settings/components/index.ts`

**Interfaces:**
- Produces exports: `SettingsTab`, `SettingsSection`, `SettingsCard`, `SettingsRow`, `SettingsSaveState`, `SettingsSaveStatus`, `SettingsControlSize`
- Produces: `useAutoSave<T>(options): { status, flush, saveNow }`

- [ ] **Step 1: Port `use-auto-save.ts` từ Multica**

Copy logic từ `multica/packages/views/settings/components/use-auto-save.ts` — đổi import path nếu cần. Port kèm test file.

- [ ] **Step 2: Port `settings-layout.tsx`**

Copy từ Multica; token swaps:
- `divide-surface-border` → `divide-border`
- Giữ `text-title-lg`, `text-body`, `text-caption`, `text-muted-foreground`, `text-success`, `text-destructive`

- [ ] **Step 3: Layout test**

```tsx
it("SettingsSaveState exposes role=status", () => {
  render(<SettingsSaveState status="saved" savingLabel="…" savedLabel="Saved" errorLabel="…" />);
  expect(screen.getByRole("status")).toHaveTextContent("Saved");
});
```

- [ ] **Step 4: Run tests**

Run: `pnpm --filter @uniwork/views test settings`

- [ ] **Step 5: Commit**

```bash
git commit -m "feat(views): add settings layout primitives and auto-save hook"
```

---

### Task 5: settings-page shell + i18n

**Files:**
- Create: `packages/views/settings/components/settings-page.tsx`
- Create: `packages/views/settings/components/settings-page.test.tsx`
- Modify: `packages/core/i18n/locales/vi.json`, `en.json`
- Modify: `packages/views/settings/components/index.ts`

**Interfaces:**
- Produces: `SettingsPage` — 5 tabs, `?tab=` whitelist, `navigation.replace`
- Tab stubs: import real tabs ở task sau; task này mock stub như Multica test

- [ ] **Step 1: Thêm i18n keys (vi + en parity)**

```json
"settings": {
  "page": {
    "title": "Cài đặt",
    "my_account": "Tài khoản",
    "workspace_fallback": "Workspace",
    "tabs": {
      "profile": "Hồ sơ",
      "preferences": "Tùy chọn",
      "workspace": "Chung",
      "members": "Thành viên",
      "integrations": "Tích hợp"
    }
  }
},
"nav": { "settings": "Cài đặt" }
```

- [ ] **Step 2: Port `settings-page.tsx` (rút tab)**

Account keys: `profile`, `preferences`
Workspace keys: `workspace`, `members`, `integrations`
Bỏ feature flags. Dùng `useTranslation("settings")`, `useWorkspace()` cho workspace name header.

`CollapsedNavTrigger` từ `../../layout/page-header`.

- [ ] **Step 3: Shell test (stub tabs)**

Mock `./account-tab` etc. Test:
- unknown `?tab=evil` → renders profile
- tab click calls `navigation.replace` with `?tab=preferences`
- compact width shows sidebar trigger

- [ ] **Step 4: Run tests + lint**

Run: `pnpm --filter @uniwork/views test settings-page && pnpm --filter @uniwork/core test parity`

- [ ] **Step 5: Commit**

```bash
git commit -m "feat(views): add settings page shell and i18n keys"
```

---

## Phase D — Tab implementations

### Task 6: account-tab + preferences-tab

**Files:**
- Create: `packages/views/settings/components/account-tab.tsx`
- Create: `packages/views/settings/components/account-tab.test.tsx`
- Create: `packages/views/settings/components/preferences-tab.tsx`
- Create: `packages/views/settings/components/preferences-tab.test.tsx`

- [ ] **Step 1: account-tab test**

Mock `@uniwork/core/api/http`. Assert auto-save calls PATCH after debounce; avatar upload calls POST multipart.

- [ ] **Step 2: Implement account-tab**

- `useAuthStore` user + `setUser`
- `useAutoSave` on `{ displayName }` vs saved from user
- Hidden file input → `uploadAvatar` → `setUser`
- Email read-only `SettingsRow`
- `SettingsSaveState` with `t("settings.save.*")`

- [ ] **Step 3: preferences-tab test**

Theme select fires `setTheme`; language select calls `localeAdapter.persist` (mock adapter context).

- [ ] **Step 4: Implement preferences-tab**

- `useTheme` from `@uniwork/ui/components/common/theme-provider`
- `useLocaleAdapter` + `SUPPORTED_LOCALES` from `@uniwork/core/i18n`
- Language change: persist + `window.location.reload()`

- [ ] **Step 5: Wire into settings-page** (unmock real tabs)

- [ ] **Step 6: Commit**

```bash
git commit -m "feat(views): add account and preferences settings tabs"
```

---

### Task 7: workspace-tab + members-tab + integrations-tab

**Files:**
- Create: `workspace-tab.tsx`, `workspace-tab.test.tsx`
- Create: `members-tab.tsx`, `members-tab.test.tsx`
- Create: `integrations-tab.tsx`, `integrations-tab.test.tsx`
- Modify: `packages/views/workspace/members-view.tsx`

- [ ] **Step 1: Refactor MembersView**

```tsx
export function MembersView({ workspaceId, embedded = false }: { workspaceId: string; embedded?: boolean }) {
  // when embedded: no outer max-w/p-6, no h1 — panel only
}
```

- [ ] **Step 2: workspace-tab**

- `useWorkspace()` from layout context
- `useWorkspacePermissions(wsId).canUpdateWorkspaceSettings` (expose từ hook wrapper)
- Admin: editable name + auto-save via `usePatchWorkspace`
- Member: read-only name

- [ ] **Step 3: members-tab**

```tsx
export function MembersTab() {
  const { workspace } = useWorkspace();
  return (
    <SettingsTab title={t("settings.page.tabs.members")} description={…}>
      <MembersView workspaceId={workspace.id} embedded />
    </SettingsTab>
  );
}
```

- [ ] **Step 4: integrations-tab**

Empty state only — `Plug` icon, `t("settings.integrations.emptyTitle")`, `emptyDescription`. Không list fake connectors.

- [ ] **Step 5: Tests for all three tabs**

- [ ] **Step 6: Commit**

```bash
git commit -m "feat(views): add workspace, members, and integrations settings tabs"
```

---

## Phase E — Wiring

### Task 8: Routes, sidebar, redirect

**Files:**
- Create: `apps/web/app/[orgSlug]/[workspaceSlug]/settings/page.tsx`
- Modify: `apps/web/app/[orgSlug]/[workspaceSlug]/members/page.tsx`
- Modify: `packages/views/layout/app-sidebar.tsx`
- Modify: `packages/views/layout/app-sidebar.test.tsx`
- Optional: `e2e/settings.spec.ts`

- [ ] **Step 1: Settings route**

```tsx
"use client";
import { SettingsPage } from "@uniwork/views/settings";

export default function Page() {
  return <SettingsPage />;
}
```

- [ ] **Step 2: Members redirect**

```tsx
import { redirect } from "next/navigation";
// hoặc client replace — dùng paths builder:
// redirect(`/${orgSlug}/${workspaceSlug}/settings?tab=members`)
```

Prefer Next `redirect()` in server component page reading params, hoặc thin client `useEffect` + `useNavigation().replace`.

- [ ] **Step 3: Sidebar changes**

Remove Members from `items` array (2 items only).
Add to dropdown before logout:

```tsx
<DropdownMenuItem render={<AppLink href={ws.settings()} onClick={dismissSheet} />}>
  <Settings aria-hidden />
  {t("nav.settings")}
</DropdownMenuItem>
```

Import `Settings` from lucide-react.

- [ ] **Step 4: Update app-sidebar.test.tsx**

- Expect links: Công việc, Cuộc họp (not Thành viên)
- Open account menu → có "Cài đặt" link tới `/acme/team/settings`

- [ ] **Step 5: paths consistency**

Thêm settings page → `make check` paths test passes (chạy sau khi page tồn tại).

- [ ] **Step 6: Verification**

```bash
pnpm typecheck
pnpm test
make test-go
```

Optional e2e: rename workspace in settings → sidebar switcher shows new name.

- [ ] **Step 7: Commit**

```bash
git commit -m "feat(web): wire settings route, sidebar entry, and members redirect"
```

---

## Self-Review (spec coverage)

| Spec § | Task |
|--------|------|
| PATCH /me | Task 1, 3, 6 |
| PATCH workspace | Task 2, 3, 7 |
| uploadAvatar FE | Task 3, 6 |
| settings-layout / auto-save | Task 4 |
| 5 tabs IA | Task 5, 6, 7 |
| Members embed | Task 7 |
| Integrations empty | Task 7 |
| Sidebar 2 nav items | Task 8 |
| Settings dropdown entry | Task 8 |
| /members redirect | Task 8 |
| paths.settings() | Task 3, 8 |
| canUpdateWorkspaceSettings | Task 3, 7 |
| i18n vi/en | Task 5, 6, 7 |
| no-usf-leak | All — no multica string in comments |

No TBD placeholders remain in task steps above.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-08-27-settings.md`. Two execution options:

**1. Subagent-Driven (recommended)** — dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** — implement tasks in this session with checkpoints between phases

Which approach?
