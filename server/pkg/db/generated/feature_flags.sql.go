// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: feature_flags.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const adminListAllFlagOverrides = `-- name: AdminListAllFlagOverrides :many
SELECT id, flag_key, scope_type, scope_id, enabled, note, created_by, created_by_kind, created_at, expires_at FROM feature_flag_overrides ORDER BY flag_key, scope_type, scope_id
`

// The console's Flags screen: every override in one round trip, so a
// catalogue of N flags costs one query instead of N.
func (q *Queries) AdminListAllFlagOverrides(ctx context.Context) ([]FeatureFlagOverride, error) {
	rows, err := q.db.Query(ctx, adminListAllFlagOverrides)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []FeatureFlagOverride{}
	for rows.Next() {
		var i FeatureFlagOverride
		if err := rows.Scan(
			&i.ID,
			&i.FlagKey,
			&i.ScopeType,
			&i.ScopeID,
			&i.Enabled,
			&i.Note,
			&i.CreatedBy,
			&i.CreatedByKind,
			&i.CreatedAt,
			&i.ExpiresAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const countFlagOverridesByKey = `-- name: CountFlagOverridesByKey :many
SELECT flag_key, count(*)::bigint AS n FROM feature_flag_overrides
WHERE expires_at IS NULL OR expires_at > now()
GROUP BY flag_key
`

type CountFlagOverridesByKeyRow struct {
	FlagKey string `json:"flag_key"`
	N       int64  `json:"n"`
}

func (q *Queries) CountFlagOverridesByKey(ctx context.Context) ([]CountFlagOverridesByKeyRow, error) {
	rows, err := q.db.Query(ctx, countFlagOverridesByKey)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []CountFlagOverridesByKeyRow{}
	for rows.Next() {
		var i CountFlagOverridesByKeyRow
		if err := rows.Scan(&i.FlagKey, &i.N); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const deleteFlagOverride = `-- name: DeleteFlagOverride :execrows
DELETE FROM feature_flag_overrides WHERE flag_key = $1 AND scope_type = $2 AND scope_id = $3
`

type DeleteFlagOverrideParams struct {
	FlagKey   string `json:"flag_key"`
	ScopeType string `json:"scope_type"`
	ScopeID   string `json:"scope_id"`
}

func (q *Queries) DeleteFlagOverride(ctx context.Context, arg DeleteFlagOverrideParams) (int64, error) {
	result, err := q.db.Exec(ctx, deleteFlagOverride, arg.FlagKey, arg.ScopeType, arg.ScopeID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getFlagOverride = `-- name: GetFlagOverride :one
SELECT id, flag_key, scope_type, scope_id, enabled, note, created_by, created_by_kind, created_at, expires_at FROM feature_flag_overrides WHERE flag_key = $1 AND scope_type = $2 AND scope_id = $3
`

type GetFlagOverrideParams struct {
	FlagKey   string `json:"flag_key"`
	ScopeType string `json:"scope_type"`
	ScopeID   string `json:"scope_id"`
}

func (q *Queries) GetFlagOverride(ctx context.Context, arg GetFlagOverrideParams) (FeatureFlagOverride, error) {
	row := q.db.QueryRow(ctx, getFlagOverride, arg.FlagKey, arg.ScopeType, arg.ScopeID)
	var i FeatureFlagOverride
	err := row.Scan(
		&i.ID,
		&i.FlagKey,
		&i.ScopeType,
		&i.ScopeID,
		&i.Enabled,
		&i.Note,
		&i.CreatedBy,
		&i.CreatedByKind,
		&i.CreatedAt,
		&i.ExpiresAt,
	)
	return i, err
}

const listActiveFlagOverrides = `-- name: ListActiveFlagOverrides :many

SELECT id, flag_key, scope_type, scope_id, enabled, note, created_by, created_by_kind, created_at, expires_at FROM feature_flag_overrides
WHERE expires_at IS NULL OR expires_at > now()
ORDER BY flag_key, scope_type, scope_id
`

// Feature flag overrides (F-11 §7). The DB provider reads the whole table
// into a 30 s cache; the admin service writes it.
func (q *Queries) ListActiveFlagOverrides(ctx context.Context) ([]FeatureFlagOverride, error) {
	rows, err := q.db.Query(ctx, listActiveFlagOverrides)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []FeatureFlagOverride{}
	for rows.Next() {
		var i FeatureFlagOverride
		if err := rows.Scan(
			&i.ID,
			&i.FlagKey,
			&i.ScopeType,
			&i.ScopeID,
			&i.Enabled,
			&i.Note,
			&i.CreatedBy,
			&i.CreatedByKind,
			&i.CreatedAt,
			&i.ExpiresAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listFlagOverridesByKey = `-- name: ListFlagOverridesByKey :many
SELECT id, flag_key, scope_type, scope_id, enabled, note, created_by, created_by_kind, created_at, expires_at FROM feature_flag_overrides WHERE flag_key = $1 ORDER BY scope_type, scope_id
`

func (q *Queries) ListFlagOverridesByKey(ctx context.Context, flagKey string) ([]FeatureFlagOverride, error) {
	rows, err := q.db.Query(ctx, listFlagOverridesByKey, flagKey)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []FeatureFlagOverride{}
	for rows.Next() {
		var i FeatureFlagOverride
		if err := rows.Scan(
			&i.ID,
			&i.FlagKey,
			&i.ScopeType,
			&i.ScopeID,
			&i.Enabled,
			&i.Note,
			&i.CreatedBy,
			&i.CreatedByKind,
			&i.CreatedAt,
			&i.ExpiresAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertFlagOverride = `-- name: UpsertFlagOverride :one
INSERT INTO feature_flag_overrides (id, flag_key, scope_type, scope_id, enabled, note, created_by, created_by_kind, expires_at)
VALUES ($1, $2, $3, $4, $5, $6, $7, 'human', $8)
ON CONFLICT (flag_key, scope_type, scope_id) DO UPDATE SET
  enabled = EXCLUDED.enabled, note = EXCLUDED.note, created_by = EXCLUDED.created_by,
  created_at = now(), expires_at = EXCLUDED.expires_at
RETURNING id, flag_key, scope_type, scope_id, enabled, note, created_by, created_by_kind, created_at, expires_at
`

type UpsertFlagOverrideParams struct {
	ID        string             `json:"id"`
	FlagKey   string             `json:"flag_key"`
	ScopeType string             `json:"scope_type"`
	ScopeID   string             `json:"scope_id"`
	Enabled   bool               `json:"enabled"`
	Note      string             `json:"note"`
	CreatedBy string             `json:"created_by"`
	ExpiresAt pgtype.Timestamptz `json:"expires_at"`
}

func (q *Queries) UpsertFlagOverride(ctx context.Context, arg UpsertFlagOverrideParams) (FeatureFlagOverride, error) {
	row := q.db.QueryRow(ctx, upsertFlagOverride,
		arg.ID,
		arg.FlagKey,
		arg.ScopeType,
		arg.ScopeID,
		arg.Enabled,
		arg.Note,
		arg.CreatedBy,
		arg.ExpiresAt,
	)
	var i FeatureFlagOverride
	err := row.Scan(
		&i.ID,
		&i.FlagKey,
		&i.ScopeType,
		&i.ScopeID,
		&i.Enabled,
		&i.Note,
		&i.CreatedBy,
		&i.CreatedByKind,
		&i.CreatedAt,
		&i.ExpiresAt,
	)
	return i, err
}
