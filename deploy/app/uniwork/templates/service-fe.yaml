apiVersion: v1
kind: Service
metadata:
  name: uniwork-fe
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: uniwork-fe
  ports:
    - name: http
      port: 3000
      targetPort: http
