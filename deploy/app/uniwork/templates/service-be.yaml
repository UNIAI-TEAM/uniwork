apiVersion: v1
kind: Service
metadata:
  name: uniwork-be
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: uniwork-be
  ports:
    - name: http
      port: 8080
      targetPort: http
