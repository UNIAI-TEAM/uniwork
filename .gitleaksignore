# Findings gitleaks may skip, by exact fingerprint (commit:file:rule:line).
# Only for a secret already in shared history that cannot be rewritten, and
# only after it has been rotated. An entry here does NOT make the value safe.
#
# EMAIL_HUB_CREDENTIAL_KEY was committed to .env.example in 7256390b (Email Hub
# PR #105) and removed again in b021caaa. The value is burned and rotated: no
# environment may use it; deployments read the key from the uniwork-email-hub
# secret. Decision recorded in UNI-773 (2026-09-24).
7256390bf4d21b58770791de5e32b3627a596384:.env.example:generic-api-key:230
